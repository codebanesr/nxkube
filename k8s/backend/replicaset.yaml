apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: nestjs
  labels:
    app: nestjs
spec:
  # modify replicas according to your case
  replicas: 3
  selector:
    matchLabels:
      app: nestjs
  template:
    metadata:
      labels:
        app: nestjs
    spec:
      automountServiceAccountToken: true
      containers:
      - name: nestjs
        imagePullPolicy: Always
        image: shanurcsenitap/nxkube_backend:latest
        ports:
        - name: api
          containerPort:  8080
          protocol: TCP
      #   env:
      #   - name: NGX_VERSION
      #     value: 1.16.1
      #   volumeMounts:
      #   - name: localtime
      #     mountPath: /etc/localtime
      # volumes:
      #   - name: localtime
      #     hostPath:
      #       path: /usr/share/zoneinfo/Asia/Shanghai
